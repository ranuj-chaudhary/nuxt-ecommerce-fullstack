# Build & Deployment Runbook

## 1. Build Modes
Depending on hosting infrastructure, the application supports two compilation modes:

### Mode A: Node.js Server (SSR Fullstack)
```bash
npm run build

npm run generate
```